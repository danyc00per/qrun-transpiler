# Versions figées le 2026-07-23 sur ce qui tournait au déploiement du 22/07.
#
# POURQUOI : sans pin, chaque rebuild Render réinstalle les dernières versions.
# Qiskit publie souvent et a déjà cassé des API entre majeures ; ce service est
# le seul à transpiler pour IBM, donc une régression y coupe tout le chemin IBM.
#
# POUR METTRE À JOUR PLUS TARD : une ligne à la fois, redéployer, transpiler un
# vrai circuit pour vérifier. Jamais tout d'un coup.

fastapi==0.139.2
uvicorn[standard]==0.51.0
qiskit==2.5.0
qiskit-qasm3-import==0.6.0
